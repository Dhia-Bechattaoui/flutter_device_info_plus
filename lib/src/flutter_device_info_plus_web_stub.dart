import 'package:flutter_web_plugins/flutter_web_plugins.dart';

/// A stub method for `registerPlugin`.
/// This is used on non-web platforms to prevent compilation errors
/// when `dart:js_interop` is not available.
void registerPlugin(final Registrar registrar) {
  // No-op for non-web platforms
}
